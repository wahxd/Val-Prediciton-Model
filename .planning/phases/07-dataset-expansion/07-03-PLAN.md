---
phase: 07-dataset-expansion
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "The pipeline successfully discovers, downloads, and processes at least 1 VOD end-to-end"
    - "The manifest correctly tracks the VOD through all status transitions"
    - "The user has verified VLR.gg scraping works against live pages"
    - "Processing is queued for 30+ additional maps from at least one non-Champions tournament"
  artifacts: []
  key_links: []
---

<objective>
Verify end-to-end pipeline operation against live VLR.gg pages and queue up VODs for background processing.

Purpose: VLR.gg HTML selectors are reverse-engineered and may not work perfectly. This plan verifies the full pipeline against real pages, fixes any selector issues, and queues up the actual dataset expansion work.

Output: Verified working pipeline + manifest populated with 30+ pending VODs ready for background processing.
</objective>

<execution_context>
@C:\Users\sheva\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sheva\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dataset-expansion/07-CONTEXT.md
@.planning/phases/07-dataset-expansion/07-01-SUMMARY.md
@.planning/phases/07-dataset-expansion/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test VLR.gg scraping against live pages</name>
  <files></files>
  <action>
1. Test the VLR event scraper against a real VLR.gg event page:
   - Pick a VCT event with known matches (e.g., Masters Bangkok, VCT Americas, VCT EMEA)
   - Run the discovery step: `python scripts/expand_dataset.py "EVENT_URL" --discover-only --tournament "TEST"`
   - Check the manifest: `python scripts/summarize_progress.py --verbose`

2. If scraping fails (likely -- selectors are guessed):
   - Fetch a VLR.gg event page manually and inspect HTML structure
   - Fix CSS selectors in src/scraping/vlr_events.py to match actual page structure
   - Also verify Valoscribe's scrape_match works on a specific match URL:
     `python -m valoscribe scrape-vlr "MATCH_URL" -o data/processing/test_match.json`
   - If Valoscribe's scraper also needs fixes, update it in D:\Git\valoscribe

3. Run a single end-to-end test:
   - Pick ONE match with a working VOD URL from the manifest
   - Process just that one: modify manifest to have only 1 pending, run --process-only
   - OR: manually run the Valoscribe commands in sequence:
     a. `python -m valoscribe scrape-vlr <match_url> -o data/processing/metadata/test.json`
     b. `python -m valoscribe split-metadata data/processing/metadata/test.json --output-dir data/processing/metadata/maps/`
     c. `python -m valoscribe download <youtube_url> --output data/processing/downloads/`
     d. `python -m valoscribe orchestrate process-vod <video_path> <map_metadata.json> --output data/processing/processed/test/ --quiet`
   - Verify output: events.jsonl and frames.csv exist in output directory

4. Fix any issues found during testing. Common problems:
   - VLR.gg event page uses different HTML structure than expected
   - Match URLs are relative (need to prepend https://www.vlr.gg)
   - VOD URLs missing for some maps (not all matches have per-map VODs)
   - Valoscribe CLI arguments don't match what orchestrator sends
   - Path separators (Windows backslash issues in subprocess calls)
  </action>
  <verify>
- `python scripts/summarize_progress.py` shows at least 1 VOD with status "complete"
- Output directory contains events.jsonl and frames.csv for the completed VOD
  </verify>
  <done>
At least 1 VOD has been processed end-to-end. Pipeline verified against live VLR.gg pages. Any selector/integration issues fixed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
End-to-end VOD processing pipeline: VLR.gg event discovery -> match scraping -> VOD download -> Valoscribe processing -> manifest tracking.

One test VOD has been processed to verify the full pipeline works.
  </what-built>
  <how-to-verify>
1. Check the manifest: `python scripts/summarize_progress.py --verbose`
   - Should show at least 1 completed VOD
2. Check the output: look in data/processing/processed/ for events.jsonl and frames.csv
3. Verify the pipeline is ready for bulk processing
4. If satisfied, approve. Then Claude will queue up 30+ maps for background processing.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with queuing 30+ maps, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Queue 30+ maps for background processing</name>
  <files></files>
  <action>
1. Run discovery against at least one major VCT event:
   - `python scripts/expand_dataset.py "EVENT_URL" --discover-only --tournament "TOURNAMENT_NAME"`
   - Target events with many matches: VCT Americas/EMEA/Pacific leagues have 50-100+ matches each
   - If first event doesn't yield 30 maps with VOD URLs, add a second event

2. Verify manifest has 30+ pending VODs:
   - `python scripts/summarize_progress.py`
   - Should show 30+ total VODs with most in "pending" status

3. Start background processing:
   - `python scripts/expand_dataset.py --process-only`
   - Let it run. User can Ctrl+C at any time and resume later.
   - This will take hours/days -- it's designed to run in the background.

4. Log the initial state for the summary:
   - Total VODs queued
   - Tournament(s) selected
   - Estimated processing time
  </action>
  <verify>
`python scripts/summarize_progress.py` shows 30+ total VODs queued (pending + any already processing/complete)
  </verify>
  <done>
30+ VCT maps from non-Champions-2025 tournament(s) are queued in the manifest. Processing has been started (user can run in background). Phase 7 success criteria met.
  </done>
</task>

</tasks>

<verification>
1. Manifest contains 30+ VODs from non-Champions-2025 tournament(s)
2. At least 1 VOD has been fully processed end-to-end (events.jsonl + frames.csv exist)
3. Pipeline is resumable (can stop and restart without data loss)
4. `python scripts/summarize_progress.py` shows accurate progress
</verification>

<success_criteria>
- Pipeline verified against live VLR.gg pages (selectors work)
- At least 1 map processed end-to-end
- 30+ maps queued for background processing
- User can run `python scripts/expand_dataset.py --process-only` to continue processing
</success_criteria>

<output>
After completion, create `.planning/phases/07-dataset-expansion/07-03-SUMMARY.md`
</output>
