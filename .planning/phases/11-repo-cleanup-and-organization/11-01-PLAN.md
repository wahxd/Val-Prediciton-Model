---
phase: 11-repo-cleanup-and-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend.py
  - dashboard.py
  - vision_engine.py
  - config.py
  - nul
  - synthesize research example/
  - src/events/
  - src/state/
  - src/quality/
  - tests/test_event_emitter.py
  - tests/test_integration.py
  - tests/test_ocr_config.py
  - tests/test_replay_detector.py
  - tests/test_state_tracker.py
  - tests/test_validator.py
  - scripts/compare_baseline.py
  - scripts/expand_dataset.py
  - scripts/summarize_progress.py
  - scripts/CHECKPOINT_PREDICTION_PLAN.md
  - experiments/smoke_test/
  - experiments/smoke_test_validation/
  - experiments/checkpoint_lr/
  - experiments/checkpoint_xgb/
  - experiments/checkpoint_r6_lr/
  - experiments/checkpoint_r12_lr/
  - experiments/checkpoint_r18_lr/
  - experiments/real_lr_core/
  - experiments/real_lr_full/
  - experiments/real_xgb_core/
  - experiments/real_xgb_full/
  - data/analysis.db
  - data/processing/manifest.json
  - data/processing/manifest.v2_backup.json
autonomous: true

must_haves:
  truths:
    - "Root directory contains no stray v1 files (backend.py, dashboard.py, vision_engine.py, config.py, nul)"
    - "src/ contains no v1 event detection packages (events/, state/, quality/)"
    - "tests/ contains no v1 test files (test_event_emitter, test_validator, test_state_tracker, test_ocr_config, test_integration, test_replay_detector)"
    - "scripts/ contains only run_real_experiment.py and run_checkpoint_prediction.py"
    - "experiments/ has v2_baseline/ subdirectory with 9 experiment directories, no smoke_test directories"
    - "data/analysis.db is removed"
    - "data/processing/manifest.json is reset to empty, backup exists"
  artifacts:
    - path: "experiments/v2_baseline/"
      provides: "Archived v2 experiment results for Phase 14 comparison"
    - path: "data/processing/manifest.v2_backup.json"
      provides: "Backup of pre-reset manifest with 46 entries"
    - path: "data/processing/manifest.json"
      provides: "Empty manifest ready for Phase 12 rebuild"
      contains: "records"
  key_links: []
---

<objective>
Remove all dead code, stray files, superseded scripts, and organize experiments and data directories for v3 scaling.

Purpose: Eliminate 1,485 LOC of v1 dead code, remove root-level stray files, clean up superseded scripts, archive v2 experiments into a milestone directory, and reset the processing manifest for Phase 12 rebuild. This reduces noise and establishes clean directory conventions before the structural module reorganization in Plan 02.

Output: A codebase with only active code, organized experiments, and clean data directories.
</objective>

<execution_context>
@C:\Users\sheva\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sheva\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-repo-cleanup-and-organization/11-CONTEXT.md
@.planning/phases/11-repo-cleanup-and-organization/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stray root files, v1 modules, and superseded scripts</name>
  <files>
    backend.py, dashboard.py, vision_engine.py, config.py, nul,
    synthesize research example/,
    src/events/, src/state/, src/quality/,
    tests/test_event_emitter.py, tests/test_integration.py,
    tests/test_ocr_config.py, tests/test_replay_detector.py,
    tests/test_state_tracker.py, tests/test_validator.py,
    scripts/compare_baseline.py, scripts/expand_dataset.py,
    scripts/summarize_progress.py, scripts/CHECKPOINT_PREDICTION_PLAN.md
  </files>
  <action>
    Delete all dead code and stray files using git rm to preserve history. The v2.0 git tag already preserves all v1 code.

    Step 1 - Delete stray root files:
    ```
    git rm backend.py dashboard.py vision_engine.py config.py nul
    git rm -r "synthesize research example"
    ```
    Note: config.py at root is v1 OCR config (TESSERACT_CMD, ROI_TIMER, etc.) -- dead code, not the config files in src/ packages.

    Step 2 - Delete v1 event detection modules (1,485 LOC, zero active consumers):
    ```
    git rm -r src/events/ src/state/ src/quality/
    ```

    Step 3 - Delete associated v1 test files:
    ```
    git rm tests/test_event_emitter.py tests/test_integration.py tests/test_ocr_config.py tests/test_replay_detector.py tests/test_state_tracker.py tests/test_validator.py
    ```

    Step 4 - Delete superseded scripts:
    ```
    git rm scripts/compare_baseline.py scripts/expand_dataset.py scripts/summarize_progress.py scripts/CHECKPOINT_PREDICTION_PLAN.md
    ```

    Step 5 - Clean __pycache__ directories from deleted module locations:
    Remove any __pycache__ directories under src/events/, src/state/, src/quality/, and root __pycache__/ if present. These are gitignored so use rm -rf (not git rm):
    ```
    rm -rf __pycache__ src/events/__pycache__ src/state/__pycache__ src/quality/__pycache__
    ```

    Step 6 - Update src/__init__.py docstring from v1 description to v3:
    Change `"""Valorant VCT event detection and state management package."""` to `"""Valorant VCT match prediction model."""`

    Step 7 - Commit:
    ```
    git commit -m "refactor: remove v1 dead code, stray files, and superseded scripts

    - Delete src/events/, src/state/, src/quality/ (1,485 LOC, preserved at v2.0 tag)
    - Delete 6 associated v1 test files
    - Delete root stray files: backend.py, dashboard.py, vision_engine.py, config.py, nul
    - Delete 'synthesize research example' folder
    - Delete superseded scripts: compare_baseline.py, expand_dataset.py, summarize_progress.py, CHECKPOINT_PREDICTION_PLAN.md
    - Keep scripts/run_real_experiment.py and scripts/run_checkpoint_prediction.py (Phase 14 reference)"
    ```

    IMPORTANT: Do NOT delete anything from src/scraping/ -- that package is restructured in Plan 02.
    IMPORTANT: Do NOT delete src/data/config.py or src/modeling/config.py -- those move in Plan 02.
  </action>
  <verify>
    1. `git status` shows clean working tree (all changes committed)
    2. `ls backend.py dashboard.py vision_engine.py config.py nul 2>&1` -- all report "No such file"
    3. `ls src/events src/state src/quality 2>&1` -- all report "No such file or directory"
    4. `ls scripts/` -- shows only run_real_experiment.py and run_checkpoint_prediction.py
    5. `ls tests/test_event_emitter.py tests/test_validator.py 2>&1` -- reports "No such file"
    6. Remaining tests still collect: `python -m pytest tests/ --collect-only 2>&1 | tail -5` -- shows collected tests with no import errors
  </verify>
  <done>
    All v1 code, stray root files, and superseded scripts removed. scripts/ contains only 2 reference implementations. Remaining tests collect without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reorganize experiments into v2_baseline and clean data directory</name>
  <files>
    experiments/v2_baseline/,
    experiments/smoke_test/,
    experiments/smoke_test_validation/,
    experiments/checkpoint_lr/, experiments/checkpoint_xgb/,
    experiments/checkpoint_r6_lr/, experiments/checkpoint_r12_lr/, experiments/checkpoint_r18_lr/,
    experiments/real_lr_core/, experiments/real_lr_full/,
    experiments/real_xgb_core/, experiments/real_xgb_full/,
    data/analysis.db,
    data/processing/manifest.json,
    data/processing/manifest.v2_backup.json
  </files>
  <action>
    Step 1 - Delete smoke test directories:
    ```
    rm -rf experiments/smoke_test experiments/smoke_test_validation
    ```

    Step 2 - Create v2_baseline directory and move 9 experiment directories:
    ```
    mkdir -p experiments/v2_baseline
    git mv experiments/checkpoint_lr experiments/v2_baseline/
    git mv experiments/checkpoint_xgb experiments/v2_baseline/
    git mv experiments/checkpoint_r6_lr experiments/v2_baseline/
    git mv experiments/checkpoint_r12_lr experiments/v2_baseline/
    git mv experiments/checkpoint_r18_lr experiments/v2_baseline/
    git mv experiments/real_lr_core experiments/v2_baseline/
    git mv experiments/real_lr_full experiments/v2_baseline/
    git mv experiments/real_xgb_core experiments/v2_baseline/
    git mv experiments/real_xgb_full experiments/v2_baseline/
    ```

    Step 3 - Delete stale data/analysis.db:
    ```
    git rm data/analysis.db
    ```

    Step 4 - Backup and reset processing manifest:
    ```
    cp data/processing/manifest.json data/processing/manifest.v2_backup.json
    ```
    Then overwrite data/processing/manifest.json with: `{"records": []}`
    Stage both files:
    ```
    git add data/processing/manifest.json data/processing/manifest.v2_backup.json
    ```

    Step 5 - Commit:
    ```
    git commit -m "refactor: organize experiments by milestone and clean data directory

    - Move 9 v2 experiment dirs into experiments/v2_baseline/
    - Delete smoke_test and smoke_test_validation directories
    - Delete stale data/analysis.db
    - Backup manifest.json to manifest.v2_backup.json (46 entries, incomplete metadata)
    - Reset manifest.json to empty for Phase 12 rebuild"
    ```

    NOTE: The smoke_test directories may not be git-tracked (they could be gitignored experiment output). If `git rm -r experiments/smoke_test` fails, use `rm -rf` instead. Same for smoke_test_validation.
    NOTE: If experiment directories contain only gitignored files (e.g., .pkl models), `git mv` may fail. In that case, use regular `mv` and manually stage.
  </action>
  <verify>
    1. `ls experiments/` -- shows only v2_baseline/
    2. `ls experiments/v2_baseline/` -- shows 9 directories (checkpoint_lr, checkpoint_xgb, checkpoint_r6_lr, checkpoint_r12_lr, checkpoint_r18_lr, real_lr_core, real_lr_full, real_xgb_core, real_xgb_full)
    3. `ls data/analysis.db 2>&1` -- reports "No such file"
    4. `cat data/processing/manifest.json` -- shows `{"records": []}`
    5. `ls data/processing/manifest.v2_backup.json` -- file exists
    6. `git status` -- clean working tree
  </verify>
  <done>
    All 9 v2 experiments archived under experiments/v2_baseline/. Smoke tests deleted. data/analysis.db removed. Manifest reset to empty with backup preserved. experiments/ directory ready for future v3 experiment output.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Root directory contains: .claude/, .env.example, .git/, .gitignore, .mcp.json, .planning/, .pytest_cache/, CLAUDE.md, data/, docs/, experiments/, requirements.txt, scripts/, src/, tests/ -- NO stray files
2. src/ contains exactly: __init__.py, data/, features/, modeling/, scraping/ -- NO events/, state/, quality/
3. scripts/ contains exactly: run_real_experiment.py, run_checkpoint_prediction.py
4. experiments/ contains exactly: v2_baseline/ (with 9 subdirectories)
5. `python -m pytest tests/ --collect-only` succeeds with no import errors
6. All changes committed in 2 clean commits
</verification>

<success_criteria>
- CLEAN-01: Root and scripts/ contain only active files (stray files removed, superseded scripts deleted)
- CLEAN-02: Corrupted/unused files removed (analysis.db deleted; temp_verify.txt and =1.11/=3.0 already absent)
- CLEAN-04: data/ directory cleaned (manifest reset, analysis.db removed, backup preserved)
- CLEAN-05: Experiment files organized (9 real experiments in v2_baseline/, smoke tests deleted)
- Test collection passes with no import errors from deleted modules
</success_criteria>

<output>
After completion, create `.planning/phases/11-repo-cleanup-and-organization/11-01-SUMMARY.md`
</output>
