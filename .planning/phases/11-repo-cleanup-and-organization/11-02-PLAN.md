---
phase: 11-repo-cleanup-and-organization
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/__init__.py
  - src/pipeline/__init__.py
  - src/pipeline/manifest.py
  - src/pipeline/orchestrator.py
  - src/config/__init__.py
  - src/config/data.py
  - src/config/modeling.py
  - src/config/processing.py
  - src/scraping/__init__.py
  - src/scraping/vlr_events.py
  - src/scraping/orchestrator.py
  - src/scraping/config.py
  - src/scraping/manifest.py
  - src/data/__init__.py
  - src/data/config.py
  - src/data/cli.py
  - src/modeling/__init__.py
  - src/modeling/config.py
  - src/modeling/baseline.py
  - src/modeling/calibration.py
  - src/modeling/experiment.py
  - src/modeling/tuning.py
  - tests/test_scraping/test_manifest.py
  - tests/test_scraping/test_vlr_events.py
  - tests/pipeline/__init__.py
  - tests/pipeline/test_manifest.py
  - tests/config/__init__.py
  - tests/config/test_config.py
autonomous: true

must_haves:
  truths:
    - "src/ has exactly 6 packages: config, data, features, modeling, pipeline, scraping"
    - "src/pipeline/ contains manifest.py and orchestrator.py (moved from scraping)"
    - "src/config/ contains data.py, modeling.py, and processing.py (consolidated from 3 domain configs)"
    - "src/scraping/ contains only vlr_events.py (web scraping only)"
    - "All imports resolve correctly across src/, tests/, and scripts/"
    - "Full test suite passes with no import errors"
  artifacts:
    - path: "src/pipeline/__init__.py"
      provides: "Pipeline package exports"
      exports: ["ProcessingManifest", "VODRecord", "VODOrchestrator"]
    - path: "src/pipeline/manifest.py"
      provides: "VOD processing manifest management"
      contains: "class ProcessingManifest"
    - path: "src/pipeline/orchestrator.py"
      provides: "VOD processing orchestration"
      contains: "class VODOrchestrator"
    - path: "src/config/__init__.py"
      provides: "Centralized config exports"
      exports: ["DataPipelineConfig", "ModelConfig", "ExperimentConfig", "ProcessingConfig"]
    - path: "src/config/data.py"
      provides: "Data pipeline configuration"
      contains: "class DataPipelineConfig"
    - path: "src/config/modeling.py"
      provides: "Model and experiment configuration"
      contains: "class ModelConfig"
    - path: "src/config/processing.py"
      provides: "VOD processing pipeline configuration"
      contains: "class ProcessingConfig"
    - path: "src/scraping/__init__.py"
      provides: "Scraping package exports (VLREventScraper only)"
      exports: ["VLREventScraper"]
  key_links:
    - from: "src/scraping/vlr_events.py"
      to: "src/pipeline/manifest.py"
      via: "import ProcessingManifest, VODRecord"
      pattern: "from src\\.pipeline\\.manifest import"
    - from: "src/pipeline/orchestrator.py"
      to: "src/config/processing.py"
      via: "import ProcessingConfig"
      pattern: "from src\\.config\\.processing import ProcessingConfig"
    - from: "src/pipeline/orchestrator.py"
      to: "src/scraping/vlr_events.py"
      via: "import VLREventScraper"
      pattern: "from src\\.scraping\\.vlr_events import VLREventScraper"
    - from: "src/modeling/baseline.py"
      to: "src/config/modeling.py"
      via: "import ModelConfig"
      pattern: "from src\\.config\\.modeling import ModelConfig"
    - from: "src/data/cli.py"
      to: "src/config/data.py"
      via: "import get_config"
      pattern: "from src\\.config\\.data import get_config"
---

<objective>
Reorganize src/ module structure: split scraping into scraping + pipeline, centralize configuration into src/config/, update all imports, and verify the full test suite passes.

Purpose: Establish the 6-package module structure (config, data, features, modeling, pipeline, scraping) that separates VLR.gg web scraping (Phase 12) from VOD processing orchestration (Phase 13) and provides centralized configuration access. This is the structural foundation for all v3 feature development.

Output: Clean module boundaries with all imports resolved and tests passing.
</objective>

<execution_context>
@C:\Users\sheva\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sheva\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-repo-cleanup-and-organization/11-CONTEXT.md
@.planning/phases/11-repo-cleanup-and-organization/11-RESEARCH.md
@.planning/phases/11-repo-cleanup-and-organization/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/pipeline/ package and move files from src/scraping/</name>
  <files>
    src/pipeline/__init__.py,
    src/pipeline/manifest.py,
    src/pipeline/orchestrator.py,
    src/scraping/__init__.py,
    src/scraping/vlr_events.py,
    src/scraping/orchestrator.py,
    src/scraping/config.py,
    src/scraping/manifest.py
  </files>
  <action>
    Step 1 - Create the pipeline package directory:
    ```
    mkdir -p src/pipeline
    ```

    Step 2 - Move manifest.py and orchestrator.py from scraping to pipeline using git mv:
    ```
    git mv src/scraping/manifest.py src/pipeline/manifest.py
    git mv src/scraping/orchestrator.py src/pipeline/orchestrator.py
    ```

    Step 3 - Create src/pipeline/__init__.py:
    ```python
    """VOD processing pipeline orchestration."""
    from src.pipeline.manifest import ProcessingManifest, VODRecord
    from src.pipeline.orchestrator import VODOrchestrator

    __all__ = [
        "ProcessingManifest",
        "VODRecord",
        "VODOrchestrator",
    ]
    ```

    Step 4 - Update imports INSIDE src/pipeline/orchestrator.py:
    Change all `from src.scraping.config import ProcessingConfig` to `from src.config.processing import ProcessingConfig`
    Change all `from src.scraping.manifest import ...` to `from src.pipeline.manifest import ...`
    Keep `from src.scraping.vlr_events import VLREventScraper` as-is (VLREventScraper stays in scraping).

    Step 5 - Update imports INSIDE src/scraping/vlr_events.py:
    Change `from src.scraping.manifest import ProcessingManifest, VODRecord` to `from src.pipeline.manifest import ProcessingManifest, VODRecord`

    Step 6 - Update src/scraping/__init__.py to only export VLREventScraper:
    ```python
    """VLR.gg web scraping for match data and VOD links."""
    from src.scraping.vlr_events import VLREventScraper

    __all__ = ["VLREventScraper"]
    ```
    Note: src/scraping/config.py will be moved in Task 2 (config centralization). Do NOT delete it yet -- just remove it from __init__.py exports.

    Step 7 - Clean __pycache__ in scraping directory:
    ```
    rm -rf src/scraping/__pycache__
    ```

    Do NOT commit yet -- Task 2 will also modify these files and we want a single coherent commit.
  </action>
  <verify>
    1. `ls src/pipeline/` -- shows __init__.py, manifest.py, orchestrator.py
    2. `ls src/scraping/` -- shows __init__.py, vlr_events.py, config.py (config.py moves in Task 2)
    3. `python -c "from src.pipeline.manifest import ProcessingManifest, VODRecord; print('pipeline manifest OK')"` -- imports work
    4. Grep src/pipeline/orchestrator.py for `from src.scraping.config` -- should NOT appear (replaced with src.config.processing)
    5. Grep src/scraping/vlr_events.py for `from src.scraping.manifest` -- should NOT appear (replaced with src.pipeline.manifest)
  </verify>
  <done>
    src/pipeline/ package created with manifest.py and orchestrator.py. src/scraping/ retains only vlr_events.py and config.py (config moves in Task 2). All internal imports within moved files updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Centralize configuration into src/config/ package</name>
  <files>
    src/config/__init__.py,
    src/config/data.py,
    src/config/modeling.py,
    src/config/processing.py,
    src/data/config.py,
    src/modeling/config.py,
    src/scraping/config.py
  </files>
  <action>
    Step 1 - Create the config package directory:
    ```
    mkdir -p src/config
    ```

    Step 2 - Move config files using git mv:
    ```
    git mv src/data/config.py src/config/data.py
    git mv src/modeling/config.py src/config/modeling.py
    git mv src/scraping/config.py src/config/processing.py
    ```

    Step 3 - Create src/config/__init__.py:
    ```python
    """Centralized configuration for all project domains."""
    from src.config.data import DataPipelineConfig, get_config
    from src.config.modeling import ModelConfig, ExperimentConfig
    from src.config.processing import ProcessingConfig

    __all__ = [
        "DataPipelineConfig",
        "get_config",
        "ModelConfig",
        "ExperimentConfig",
        "ProcessingConfig",
    ]
    ```

    Step 4 - Update ALL imports across the codebase. These are the exact files that need updating:

    **src/data/cli.py:**
    Change `from src.data.config import get_config` to `from src.config.data import get_config`

    **src/modeling/baseline.py:**
    Change `from src.modeling.config import ModelConfig` to `from src.config.modeling import ModelConfig`

    **src/modeling/calibration.py:**
    Change `from src.modeling.config import ModelConfig` to `from src.config.modeling import ModelConfig`

    **src/modeling/experiment.py:**
    Change `from src.modeling.config import ExperimentConfig` to `from src.config.modeling import ExperimentConfig`

    **src/modeling/tuning.py:**
    Change `from src.modeling.config import ModelConfig` to `from src.config.modeling import ModelConfig`

    **tests/data/test_schemas.py:**
    Change `from src.data.config import DataPipelineConfig, get_config` to `from src.config.data import DataPipelineConfig, get_config`

    **tests/modeling/test_config.py:**
    Change `from src.modeling.config import ModelConfig, ExperimentConfig` to `from src.config.modeling import ModelConfig, ExperimentConfig`

    **tests/modeling/test_baseline.py:**
    Change `from src.modeling.config import ModelConfig` to `from src.config.modeling import ModelConfig`

    **tests/modeling/test_calibration.py:**
    Change `from src.modeling.config import ModelConfig` to `from src.config.modeling import ModelConfig`

    **tests/modeling/test_evaluation.py:**
    Change `from src.modeling.config import ModelConfig, ExperimentConfig` to `from src.config.modeling import ModelConfig, ExperimentConfig`

    **tests/modeling/test_experiment.py:**
    Change `from src.modeling.config import ModelConfig, ExperimentConfig` to `from src.config.modeling import ModelConfig, ExperimentConfig`

    **tests/modeling/test_tuning.py:**
    Change `from src.modeling.config import ModelConfig` to `from src.config.modeling import ModelConfig`

    **DO NOT update scripts/run_real_experiment.py or scripts/run_checkpoint_prediction.py.** Per CONTEXT.md (Claude's discretion): leave script imports as-is since they will need Phase 14 updates anyway. They import `from src.modeling.config import ...` which will break, but they are reference implementations not actively run.

    Step 5 - Move test files to match new structure:

    Move manifest tests from tests/test_scraping/ to tests/pipeline/:
    ```
    mkdir -p tests/pipeline
    git mv tests/test_scraping/test_manifest.py tests/pipeline/test_manifest.py
    ```
    Create tests/pipeline/__init__.py (empty or with docstring).

    Update imports in tests/pipeline/test_manifest.py:
    Change `from src.scraping.manifest import ProcessingManifest, VODRecord` to `from src.pipeline.manifest import ProcessingManifest, VODRecord`

    Update imports in tests/test_scraping/test_vlr_events.py:
    Change `from src.scraping.manifest import ProcessingManifest, VODRecord` to `from src.pipeline.manifest import ProcessingManifest, VODRecord`
    (VLREventScraper import stays as `from src.scraping.vlr_events import ...`)

    Create tests/config/ directory with __init__.py:
    ```
    mkdir -p tests/config
    ```
    Create tests/config/__init__.py (empty or with docstring).

    Move the config test:
    ```
    git mv tests/modeling/test_config.py tests/config/test_config.py
    ```
    The import in test_config.py was already updated to `from src.config.modeling import ...` in Step 4.

    Also add a simple test for DataPipelineConfig to tests/config/test_config.py or create tests/config/test_data_config.py that tests the data config import works. Simplest: just verify the import resolves.

    Step 6 - Clean all __pycache__ directories:
    ```
    Remove __pycache__ from: src/data/, src/modeling/, src/scraping/, tests/test_scraping/, tests/modeling/, tests/data/
    ```

    Step 7 - Verify src/ has exactly 6 packages:
    ```
    ls -d src/*/
    ```
    Should show: config/, data/, features/, modeling/, pipeline/, scraping/ (plus __pycache__/ which is gitignored).

    Step 8 - Commit all changes from Task 1 and Task 2 together:
    ```
    git add -A
    git commit -m "refactor: reorganize module structure for v3

    Module splits:
    - Create src/pipeline/ (manifest.py, orchestrator.py from src/scraping/)
    - Create src/config/ (data.py, modeling.py, processing.py from domain packages)

    Package boundaries:
    - src/scraping/ retains only VLREventScraper (VLR.gg web scraping)
    - src/pipeline/ owns VODOrchestrator + ProcessingManifest (VOD processing)
    - src/config/ centralizes DataPipelineConfig, ModelConfig, ExperimentConfig, ProcessingConfig

    Test reorganization:
    - tests/pipeline/ for manifest tests
    - tests/config/ for config tests

    Import updates across 12 source files and 7 test files
    Scripts left as-is (Phase 14 reference implementations)"
    ```
  </action>
  <verify>
    1. `ls -d src/*/` -- shows exactly: config/, data/, features/, modeling/, pipeline/, scraping/ (plus __pycache__/ if present)
    2. `python -c "from src.config import DataPipelineConfig, ModelConfig, ExperimentConfig, ProcessingConfig; print('all configs OK')"` -- all config imports work
    3. `python -c "from src.pipeline import ProcessingManifest, VODRecord, VODOrchestrator; print('pipeline OK')"` -- pipeline imports work
    4. `python -c "from src.scraping import VLREventScraper; print('scraping OK')"` -- scraping import works
    5. `python -m pytest tests/ --collect-only 2>&1 | tail -10` -- all tests collect with no import errors
    6. `python -m pytest tests/ -x --tb=short` -- full test suite passes
    7. Grep entire src/ and tests/ for `from src.scraping.manifest` or `from src.scraping.config` or `from src.scraping.orchestrator` -- should return zero results
    8. Grep entire src/ and tests/ for `from src.data.config` or `from src.modeling.config` -- should return zero results (scripts/ excluded intentionally)
  </verify>
  <done>
    src/ has exactly 6 active packages: config, data, features, modeling, pipeline, scraping. All imports across src/ and tests/ updated and resolving. Full test suite passes. Module boundaries match Phase 12 (scraping) and Phase 13 (pipeline) separation.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls -d src/*/` shows exactly 6 packages: config, data, features, modeling, pipeline, scraping
2. `python -m pytest tests/ -x --tb=short` -- full test suite passes with zero import errors
3. `python -c "from src.config import DataPipelineConfig, ModelConfig, ProcessingConfig; print('OK')"` -- centralized config works
4. `python -c "from src.pipeline import ProcessingManifest, VODOrchestrator; print('OK')"` -- pipeline package works
5. `python -c "from src.scraping import VLREventScraper; print('OK')"` -- scraping package works (only VLREventScraper)
6. No stale imports: `grep -r "from src.scraping.manifest\|from src.scraping.config\|from src.scraping.orchestrator\|from src.data.config\|from src.modeling.config" src/ tests/` returns nothing
7. `git log --oneline -2` shows clean commit(s) for module reorganization
</verification>

<success_criteria>
- CLEAN-03: src/ module structure has clear separation of concerns with 6 packages
- src/scraping/ contains only VLR.gg web scraping code
- src/pipeline/ contains VOD processing orchestration (manifest + orchestrator)
- src/config/ centralizes all configuration (data, modeling, processing)
- All imports resolve correctly
- Full test suite passes
- Test directory mirrors src/ structure (tests/pipeline/, tests/config/)
</success_criteria>

<output>
After completion, create `.planning/phases/11-repo-cleanup-and-organization/11-02-SUMMARY.md`
</output>
