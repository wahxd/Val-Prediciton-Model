---
phase: 10-advanced-model-series-retrain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modeling/config.py
  - src/modeling/baseline.py
  - src/modeling/calibration.py
  - requirements.txt
  - tests/modeling/test_baseline.py
  - tests/modeling/test_config.py
autonomous: true

must_haves:
  truths:
    - "XGBClassifier with max_depth=3-4 and min_child_weight=5-10 can be instantiated from ModelConfig"
    - "XGBoostTrainer.model_factory() returns fresh unfitted XGBClassifier instances for temporal CV"
    - "XGBoost models are wrapped in CalibratedClassifierCV exactly like logistic regression baseline"
    - "xgboost, optuna, scipy are listed in requirements.txt"
  artifacts:
    - path: "src/modeling/baseline.py"
      provides: "XGBoostTrainer class with train(), predict_proba(), model_factory() matching BaselineTrainer interface"
      contains: "class XGBoostTrainer"
    - path: "src/modeling/config.py"
      provides: "Extended ModelConfig with XGBoost hyperparameters (max_depth, min_child_weight, n_estimators, learning_rate, subsample, colsample_bytree, reg_alpha, reg_lambda)"
      contains: "max_depth"
    - path: "src/modeling/calibration.py"
      provides: "serialize_model_to_json extended to handle XGBoost models"
      contains: "xgboost"
    - path: "requirements.txt"
      provides: "New dependencies: xgboost>=3.0, optuna>=3.0, scipy>=1.11"
      contains: "xgboost"
  key_links:
    - from: "src/modeling/baseline.py"
      to: "src/modeling/config.py"
      via: "XGBoostTrainer reads XGBoost params from ModelConfig"
      pattern: "config\\.max_depth|config\\.n_estimators"
    - from: "src/modeling/baseline.py"
      to: "src/modeling/evaluation.py"
      via: "XGBoostTrainer.model_factory() compatible with temporal_cross_validate"
      pattern: "model_factory"
---

<objective>
Add XGBoost gradient boosting as an alternative model alongside the logistic regression baseline, with very conservative regularization constraints for the small dataset (n=71-117).

Purpose: Enable XGBoost vs logistic regression comparison on the same walk-forward validation, which is the core requirement for Phase 10 model improvement.
Output: XGBoostTrainer class, extended ModelConfig, updated serialization, new dependencies installed.
</objective>

<execution_context>
@C:\Users\sheva\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sheva\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-model-series-retrain/10-CONTEXT.md
@.planning/phases/10-advanced-model-series-retrain/10-RESEARCH.md
@.planning/phases/09-baseline-model-evaluation/09-01-SUMMARY.md
@.planning/phases/09-baseline-model-evaluation/09-02-SUMMARY.md
@src/modeling/config.py
@src/modeling/baseline.py
@src/modeling/calibration.py
@src/modeling/evaluation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ModelConfig with XGBoost parameters and add XGBoostTrainer</name>
  <files>
    src/modeling/config.py
    src/modeling/baseline.py
    requirements.txt
  </files>
  <action>
    1. Add new dependencies to requirements.txt:
       - xgboost>=3.0
       - optuna>=3.0
       - scipy>=1.11

    2. Extend ModelConfig in config.py with XGBoost-specific parameters (all Optional with defaults, only used when model_type="xgboost"):
       - max_depth: int = 3 (range 2-4, very conservative)
       - min_child_weight: int = 5 (range 3-10, prevents overly specific splits)
       - n_estimators: int = 50 (range 30-100, few trees)
       - learning_rate: float = 0.1 (range 0.01-0.2)
       - subsample: float = 0.8 (range 0.7-0.95)
       - colsample_bytree: float = 0.8 (range 0.7-0.95)
       - reg_alpha: float = 0.1 (L1 regularization)
       - reg_lambda: float = 1.0 (L2 regularization)
       - penalty: str = "l2" (for logistic regression, default "l2", allowed values "l1", "l2"; needed for GridSearchCV in Plan 04)

       Add field_validators for the numeric ranges. Update validate_solver to be conditional (only validate for logistic_regression). Add penalty validator allowing "l1" and "l2".

    3. Add XGBoostTrainer class in baseline.py (same file as BaselineTrainer, they share the same interface):
       ```python
       class XGBoostTrainer:
           """XGBoost gradient boosting trainer with conservative regularization."""
           def __init__(self, config: ModelConfig): ...
           def model_factory(self) -> XGBClassifier: ...  # Returns fresh unfitted model
           def train(self, X, y) -> None: ...
           def predict_proba(self, X) -> np.ndarray: ...
           def get_feature_importances(self) -> dict: ...  # XGBoost native importances
       ```

       XGBClassifier params from config: n_estimators, max_depth, min_child_weight, learning_rate, subsample, colsample_bytree, reg_alpha, reg_lambda, objective='binary:logistic', eval_metric='logloss', random_state=config.random_state, use_label_encoder=False.

    4. Add a factory function `create_trainer(config: ModelConfig)` that returns BaselineTrainer or XGBoostTrainer based on config.model_type. This avoids hardcoding model type in experiment.py.

    Do NOT modify experiment.py yet — that happens in Plan 04 after tuning is built.
  </action>
  <verify>
    Run: `cd D:\git\Val-Prediciton-Model && pip install xgboost>=3.0 optuna>=3.0 scipy>=1.11`
    Then: `python -c "from src.modeling.config import ModelConfig; from src.modeling.baseline import XGBoostTrainer, create_trainer; mc = ModelConfig(model_type='xgboost', feature_set='core'); t = create_trainer(mc); print(type(t)); m = t.model_factory(); print(type(m))"`
    Expected: XGBoostTrainer type, XGBClassifier type.
  </verify>
  <done>
    ModelConfig accepts XGBoost parameters. XGBoostTrainer creates XGBClassifier with conservative defaults. create_trainer() dispatches correctly. New dependencies installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend model serialization for XGBoost and add tests</name>
  <files>
    src/modeling/calibration.py
    tests/modeling/test_baseline.py
    tests/modeling/test_config.py
  </files>
  <action>
    1. Extend serialize_model_to_json in calibration.py to handle XGBoostTrainer:
       - Check if trainer is BaselineTrainer or XGBoostTrainer
       - For XGBoost: serialize model using model.save_model() to a temp file, read as base64 or use model.get_booster().save_raw() for binary. Actually, simpler approach: save XGBoost config + feature importances as JSON (not full model weights, since XGBoost models aren't easily JSON-serializable like logistic regression coefficients). The JSON should include: model_type="xgboost", config, feature_importances (from get_booster().get_score()), and metadata.
       - For XGBoost: Also provide a save_xgboost_model(trainer, filepath) function that uses model.save_model(filepath) for the full binary model (XGBoost's native format, more portable than pickle).
       - Update load_model_from_json to handle model_type check gracefully (raise clear error for xgboost models, directing to load_xgboost_model instead).

    2. Add tests in test_config.py:
       - Test ModelConfig with model_type="xgboost" accepts all XGBoost params
       - Test ModelConfig with model_type="xgboost" uses correct defaults
       - Test ModelConfig validators (max_depth range, min_child_weight range, etc.)
       - Test ModelConfig with penalty="l1" and solver="saga"

    3. Add tests in test_baseline.py:
       - Test XGBoostTrainer initialization from config
       - Test XGBoostTrainer.model_factory() returns fresh XGBClassifier
       - Test XGBoostTrainer.train() and predict_proba() on synthetic data (same pattern as existing BaselineTrainer tests)
       - Test create_trainer() dispatches correctly for both model types
       - Test XGBoost model serialization round-trip

    Run all tests to ensure no regressions.
  </action>
  <verify>
    Run: `cd D:\git\Val-Prediciton-Model && python -m pytest tests/modeling/test_config.py tests/modeling/test_baseline.py -v`
    Expected: All tests pass, including new XGBoost tests.
  </verify>
  <done>
    XGBoost model serialization works. All existing tests pass. New tests cover XGBoostTrainer initialization, training, prediction, serialization, and config validation.
  </done>
</task>

</tasks>

<verification>
1. `python -c "import xgboost; import optuna; import scipy; print('Dependencies OK')"` succeeds
2. `python -c "from src.modeling.baseline import XGBoostTrainer, BaselineTrainer, create_trainer"` succeeds
3. `python -m pytest tests/modeling/ -v` passes all tests including new XGBoost tests
4. XGBoostTrainer.model_factory() is compatible with temporal_cross_validate (returns sklearn-compatible estimator)
</verification>

<success_criteria>
- XGBoostTrainer with conservative defaults (max_depth=3, min_child_weight=5, n_estimators=50) is instantiable
- model_factory() returns fresh XGBClassifier compatible with CalibratedClassifierCV wrapping
- ModelConfig frozen Pydantic schema accepts both logistic_regression and xgboost configurations
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-model-series-retrain/10-01-SUMMARY.md`
</output>
