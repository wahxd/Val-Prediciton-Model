---
phase: 01-event-detection-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/events/emitter.py
  - src/quality/metrics.py
autonomous: true

must_haves:
  truths:
    - "Kill events are emitted when alive count decreases for either team"
    - "Round end events are emitted when score increases, with win condition inferred from round context"
    - "Spike plant, defuse, and detonate events are emitted on spike_planted transitions"
    - "Round start events are emitted when timer transitions from low to high AND alive counts return to 5v5"
    - "Timeout events are emitted with team attribution when a tactical timeout is detected"
    - "Quality metrics warn at 10+ consecutive OCR failures and flag pause at 180+"
  artifacts:
    - path: "src/events/emitter.py"
      provides: "EventEmitter that transforms state changes into typed event objects including timeout detection"
      contains: "class EventEmitter"
    - path: "src/quality/metrics.py"
      provides: "QualityMetrics tracker with structured logging via structlog"
      contains: "class QualityMetrics"
  key_links:
    - from: "src/events/emitter.py"
      to: "src/events/schemas.py"
      via: "imports event classes to construct typed events"
      pattern: "from.*schemas.*import"
    - from: "src/events/emitter.py"
      to: "src/state/models.py"
      via: "receives GameState for event data"
      pattern: "from.*models.*import.*GameState"
    - from: "src/quality/metrics.py"
      to: "structlog"
      via: "structured JSON logging for quality tracking"
      pattern: "import structlog"
---

<objective>
Build the EventEmitter that converts validated state changes into typed events (including timeout detection), and QualityMetrics for tracking OCR quality and system health.

Purpose: The EventEmitter is the core logic bridge between "state changed" and "game event happened." It must correctly map state diffs to event types, infer win conditions, detect round starts via timer transitions (not thresholds), and detect tactical timeouts as explicit events with team attribution. QualityMetrics provides the observability layer required by user decisions (OCR confidence tracking, degradation warnings, replay metrics).

Output: `src/events/emitter.py` and `src/quality/metrics.py` completing the event detection pipeline (state -> validation -> debounce -> replay check -> emit -> metrics).
</objective>

<execution_context>
@C:/Users/donni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/donni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/donni/Desktop/Valorant Prediction Model/.planning/PROJECT.md
@C:/Users/donni/Desktop/Valorant Prediction Model/.planning/ROADMAP.md
@C:/Users/donni/Desktop/Valorant Prediction Model/.planning/phases/01-event-detection-foundation/01-CONTEXT.md
@C:/Users/donni/Desktop/Valorant Prediction Model/.planning/phases/01-event-detection-foundation/01-RESEARCH.md
@C:/Users/donni/Desktop/Valorant Prediction Model/.planning/phases/01-event-detection-foundation/01-01-PLAN.md
@C:/Users/donni/Desktop/Valorant Prediction Model/.planning/phases/01-event-detection-foundation/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EventEmitter with state-to-event mapping, timeout detection, and transition-based round start</name>
  <files>src/events/emitter.py</files>
  <action>
**src/events/emitter.py:**

Create class `EventEmitter`:

**Constructor:**
- `self.recent_events: deque[BaseEvent] = deque(maxlen=20)` (tracks recent events for win condition inference)
- `self.current_round_events: list[BaseEvent] = []` (events in current round, cleared on round_start)
- `self.last_spike_state: bool = False` (track previous spike_planted for transition detection)
- `self.last_timer_seconds: Optional[int] = None` (track previous timer in seconds for transition detection -- needed for round start and timeout detection)
- `self.timeout_detected: bool = False` (flag to track if current pause is a timeout vs round transition)

**Core Method:**

`emit_events(changes: dict[str, tuple[Any, Any]], current_state: GameState, previous_state: GameState) -> list[BaseEvent]`:

Takes the changes dict from StateTracker.detect_changes(), plus current and previous GameState. Returns a list of events to emit. Logic:

1. **Build base_data dict** from current_state for event construction: timestamp, frame_number, game_time (timer), game_time_seconds (converted), score_left, score_right, alive_left, alive_right, spike_planted.

2. **Check for round start** (EVNT-06 -- TRANSITION-BASED, not threshold):
   Detect round start by checking for a timer TRANSITION from low to high, not just a high timer value. Specifically:
   - Convert previous_state.timer and current_state.timer to seconds using timer_str_to_seconds
   - Round start condition: `previous_timer_seconds is not None AND previous_timer_seconds < 30 AND current_timer_seconds >= 80` (timer jumped from low to high = reset) AND `current_state.alive_left == 5 AND current_state.alive_right == 5` (full teams).
   - Also accept: `self.last_timer_seconds is not None AND self.last_timer_seconds < 30 AND current_timer_seconds >= 80` as fallback when previous_state timer may not reflect the transition.
   - When detected: emit RoundStartEvent with round_number = score_left + score_right + 1. Clear current_round_events. Reset last_spike_state to False. Reset timeout_detected to False.
   - Update `self.last_timer_seconds` after all checks.
   - This prevents false positives on replays (replay detector handles those separately) and on mid-round timer readings that happen to be high.

3. **Check for tactical timeout** (per user decision: "Tactical timeouts tracked as explicit events with team attribution"):
   Detect timeout when the game timer PAUSES or shows unusual behavior indicating a tactical timeout:
   - Condition: Timer is frozen (current_timer_seconds == previous_timer_seconds for multiple consecutive frames, tracked via a counter `self._timeout_freeze_count`) AND the game is mid-round (alive counts > 0 on both sides, no score change).
   - Alternative detection: If the timer value jumps in an unusual way that indicates timeout was called (e.g., timer pauses then resumes), detect this transition.
   - Practical approach: Track `self._timeout_freeze_count: int = 0`. When timer is identical across frames AND round is active (alive > 0 both sides), increment counter. When counter reaches a threshold (e.g., 5 frames = ~0.8s at 6fps), emit a TimeoutEvent.
   - Team attribution: Determine which team called timeout by checking which team is losing the round (fewer alive players = more likely to call timeout). If alive counts are equal, attribute to the team with the lower score. If both equal, default to "left" (imperfect but best available without additional OCR regions).
   - Set `self.timeout_detected = True` to avoid re-emitting for the same timeout.
   - Reset `self._timeout_freeze_count` when timer starts moving again.

4. **Check for kills** (EVNT-02): If alive_left decreased (new < old), emit KillEvent with team="left", alive_before=old, alive_after=new. If alive_right decreased, emit KillEvent with team="right". Note: if alive decreased by more than 1 (e.g., multi-kill between frames), emit one KillEvent with the before/after counts (not multiple single-kill events, since we can't distinguish at team-level granularity).

5. **Check for spike transitions** (EVNT-03, EVNT-04, EVNT-05):
   - If spike_planted changed from False to True: emit SpikePlantEvent with plant_time_seconds from timer.
   - If spike_planted changed from True to False: need to distinguish defuse vs detonate.
     - Check if the round just ended (score changed in same frame or recent events): if winner is the defending team, it's a defuse. If winner is attacking team, it's a detonate.
     - Heuristic: if alive counts suggest elimination happened simultaneously, lean toward detonate. If alive_left > 0 and alive_right > 0, lean toward defuse.
     - Fallback: if timer > 0 when spike un-plants, it's a defuse (spike detonation happens at timer ~0:00). If timer is near 0 (< 5 seconds), it's a detonate.
     - Emit SpikeDefuseEvent or SpikeDetonateEvent accordingly.

6. **Check for round end** (EVNT-01): If score_left or score_right increased, emit RoundEndEvent. Determine winner (whichever score increased). Infer win_condition from current_round_events using `_infer_win_condition()`:
   - If SpikeDetonateEvent in current_round_events -> "spike_detonate"
   - If SpikeDefuseEvent in current_round_events -> "spike_defuse"
   - If any team has 0 alive -> "elimination"
   - Else -> "timeout" (timer ran out with no kills/spike resolution)
   - Set final_score_left and final_score_right from current_state.

7. **Track state:** Update last_spike_state and last_timer_seconds. Append all emitted events to recent_events and current_round_events.

**Helper Methods:**

`_build_base_data(state: GameState) -> dict`: Extract common event fields from GameState.

`_infer_win_condition(current_round_events: list[BaseEvent], current_state: GameState) -> str`: Determine win condition from round event history.

`_determine_timeout_team(current_state: GameState) -> Literal["left", "right"]`: Attribute timeout to team with fewer alive players, or lower score if tied.

`reset()`: Clear recent_events, current_round_events, last_spike_state, last_timer_seconds, timeout_detected, _timeout_freeze_count (for new match).

Import from src.events.schemas (all event types including TimeoutEvent) and src.state.models (GameState). Import from src.quality.replay_detector (timer_str_to_seconds) for timer conversion.
  </action>
  <verify>
Test kill event emission: `python -c "
from src.events.emitter import EventEmitter
from src.state.models import GameState
e = EventEmitter()
prev = GameState(score_left=5, score_right=3, alive_left=4, alive_right=5, timer='1:20', spike_planted=False, frame_number=99, timestamp=1.0, ocr_confidence={})
curr = GameState(score_left=5, score_right=3, alive_left=3, alive_right=5, timer='1:18', spike_planted=False, frame_number=100, timestamp=1.1, ocr_confidence={})
changes = {'alive_left': (4, 3)}
events = e.emit_events(changes, curr, prev)
print(len(events), events[0].event_type, events[0].team)
"` should output "1 kill left".

Test round start with transition detection: `python -c "
from src.events.emitter import EventEmitter
from src.state.models import GameState
e = EventEmitter()
# Simulate end of previous round (low timer) then reset (high timer + 5v5)
prev = GameState(score_left=5, score_right=3, alive_left=0, alive_right=2, timer='0:05', spike_planted=False, frame_number=99, timestamp=1.0, ocr_confidence={})
curr = GameState(score_left=5, score_right=4, alive_left=5, alive_right=5, timer='1:40', spike_planted=False, frame_number=100, timestamp=1.1, ocr_confidence={})
changes = {'alive_left': (0, 5), 'alive_right': (2, 5), 'timer': ('0:05', '1:40'), 'score_right': (3, 4)}
events = e.emit_events(changes, curr, prev)
event_types = [ev.event_type for ev in events]
print('round_start' in event_types, 'round_end' in event_types)
"` should show round_start and round_end detected.

Test that mid-round high timer does NOT trigger round start: `python -c "
from src.events.emitter import EventEmitter
from src.state.models import GameState
e = EventEmitter()
prev = GameState(score_left=5, score_right=3, alive_left=4, alive_right=5, timer='1:25', spike_planted=False, frame_number=99, timestamp=1.0, ocr_confidence={})
curr = GameState(score_left=5, score_right=3, alive_left=4, alive_right=5, timer='1:23', spike_planted=False, frame_number=100, timestamp=1.1, ocr_confidence={})
changes = {}
events = e.emit_events(changes, curr, prev)
print('No false round_start:', len(events) == 0)
"` should output True.

Test round end with win_condition: `python -c "
from src.events.emitter import EventEmitter
from src.state.models import GameState
e = EventEmitter()
prev = GameState(score_left=5, score_right=3, alive_left=0, alive_right=2, timer='0:45', spike_planted=False, frame_number=99, timestamp=1.0, ocr_confidence={})
curr = GameState(score_left=5, score_right=4, alive_left=0, alive_right=2, timer='0:45', spike_planted=False, frame_number=100, timestamp=1.1, ocr_confidence={})
changes = {'score_right': (3, 4)}
events = e.emit_events(changes, curr, prev)
print(len(events), events[0].event_type, events[0].winner)
"` should output "1 round_end right".
  </verify>
  <done>EventEmitter correctly maps state diffs to typed events: alive decrease -> KillEvent, score increase -> RoundEndEvent with win_condition inference, spike transitions -> SpikePlant/Defuse/DetonateEvent, timer TRANSITION from low to high + 5v5 -> RoundStartEvent (not threshold-based), timer freeze mid-round -> TimeoutEvent with team attribution. Events include full game state snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Create QualityMetrics with structured logging and degradation tracking</name>
  <files>src/quality/metrics.py</files>
  <action>
**src/quality/metrics.py:**

First, configure structlog at module level for JSON-formatted structured logging:
```python
import structlog
structlog.configure(
    processors=[
        structlog.stdlib.add_log_level,
        structlog.processors.TimeStamper(fmt="iso"),
        structlog.processors.JSONRenderer()
    ],
    context_class=dict,
    logger_factory=structlog.PrintLoggerFactory(),
    cache_logger_on_first_use=True,
)
```

Create class `QualityMetrics`:

**Constructor:**
- `self.logger = structlog.get_logger("quality_metrics")`
- `self.total_frames: int = 0`
- `self.consecutive_failures: int = 0`
- `self.ocr_confidence_sum: dict[str, float]` initialized with keys: timer, score_left, score_right, alive_left, alive_right (all 0.0)
- `self.ocr_confidence_count: dict[str, int]` same keys, all 0
- `self.frames_with_valid_ocr: int = 0`
- `self.frames_skipped_low_confidence: int = 0`
- `self.frames_skipped_invalid_value: int = 0`
- `self.debounce_rejects: int = 0` (frames where value changed but didn't reach 3-frame consensus)
- `self.replay_detections: int = 0`
- `self.replay_frames_suppressed: int = 0`
- `self.events_emitted: dict[str, int]` = defaultdict(int) (count per event_type)
- `self.validation_warnings: list[str]` = [] (coherence violation log)

**Methods:**

`log_ocr_result(field: str, confidence: float, is_valid: bool) -> None`:
Track OCR quality per field. If valid and confidence >= 0.7: add to sum/count, reset consecutive_failures. If invalid: increment consecutive_failures, increment frames_skipped_low_confidence or frames_skipped_invalid_value as appropriate.
- At 10+ consecutive failures (user decision): log WARNING via structlog with field name and failure count.
- At 50+ consecutive failures (user decision: console warning for significant issues): also print to console.
- At 180+ consecutive failures (user decision: 30+ seconds at 6fps): log ERROR with recommendation to check ROI alignment or stream quality.

`log_frame_processed(valid: bool) -> None`: Increment total_frames. If valid, increment frames_with_valid_ocr.

`log_debounce_reject() -> None`: Increment debounce_rejects.

`log_replay_detected(timer_regression: str, current_score: tuple[int, int]) -> None`: Increment replay_detections. Log info via structlog with timer and score context.

`log_replay_frame_suppressed() -> None`: Increment replay_frames_suppressed.

`log_event_emitted(event_type: str) -> None`: Increment events_emitted[event_type]. Log debug via structlog.

`log_validation_warning(warning: str) -> None`: Append to validation_warnings. Log warning via structlog.

`get_summary() -> dict`: Return comprehensive metrics summary for post-stream analysis:
```python
{
    "total_frames": int,
    "frames_with_valid_ocr": int,
    "frames_skipped_low_confidence": int,
    "frames_skipped_invalid_value": int,
    "ocr_success_rate": float,  # frames_with_valid_ocr / total_frames
    "avg_confidence": {field: avg for each field},
    "debounce_rejects": int,
    "replay_detections": int,
    "replay_frames_suppressed": int,
    "events_emitted": dict,
    "total_events": int,
    "validation_warnings_count": int,
}
```

`reset() -> None`: Reset all counters (for new match).

Per user decisions:
- Console warnings for significant issues (50+ consecutive frame failures)
- Full details to log file (via structlog JSON output)
- Track per-field OCR confidence for post-stream analysis
- Track debounce statistics and replay detection triggers
  </action>
  <verify>
Test metrics creation and OCR tracking: `python -c "
from src.quality.metrics import QualityMetrics
qm = QualityMetrics()
qm.log_ocr_result('timer', 0.85, True)
qm.log_ocr_result('timer', 0.92, True)
qm.log_frame_processed(True)
qm.log_event_emitted('kill')
s = qm.get_summary()
print('Total frames:', s['total_frames'])
print('Timer avg confidence:', s['avg_confidence']['timer'])
print('Events:', s['events_emitted'])
"`.

Test warning threshold: `python -c "
from src.quality.metrics import QualityMetrics
qm = QualityMetrics()
for i in range(12):
    qm.log_ocr_result('timer', 0.3, False)
print('Consecutive failures:', qm.consecutive_failures)
"` should print 12 and show a structlog warning in output at frame 10.

Test summary: `python -c "from src.quality.metrics import QualityMetrics; qm = QualityMetrics(); print(type(qm.get_summary()))"` outputs dict.
  </verify>
  <done>QualityMetrics tracks per-field OCR confidence averages, consecutive failure counts (warns at 10, console at 50, pause-flag at 180 per user decisions), debounce statistics, replay detection counts, and per-event-type emission counts. Structured JSON logging via structlog provides full detail to log file while console gets significant issues only. get_summary() returns comprehensive dict for post-stream analysis.</done>
</task>

</tasks>

<verification>
- EventEmitter produces KillEvent when alive count decreases
- EventEmitter produces RoundEndEvent when score increases, with win_condition inferred
- EventEmitter produces SpikePlantEvent when spike_planted transitions False->True
- EventEmitter produces RoundStartEvent when timer TRANSITIONS from low (<30s) to high (>=80s) AND alive counts are 5v5 (not just threshold check)
- EventEmitter produces TimeoutEvent with team attribution when timer freezes mid-round
- EventEmitter does NOT false-positive round_start on mid-round high timer values
- QualityMetrics.get_summary() returns valid dict with all tracking fields
- QualityMetrics logs warning at 10+ consecutive OCR failures
- structlog outputs JSON-formatted log messages
</verification>

<success_criteria>
1. EventEmitter correctly maps all state change types to their corresponding event schemas
2. Round start detection uses timer TRANSITION (previous < 30s AND current >= 80s), not just threshold
3. Timeout events are emitted with team attribution when timer freezes mid-round (per user decision)
4. Win condition inference uses event history (elimination > spike > timeout logic)
5. Kill events track alive_before and alive_after per team
6. Spike events include round timer at event time per user decision
7. QualityMetrics warns at 10 consecutive failures, flags pause at 180 per user decisions
8. QualityMetrics tracks per-field OCR confidence for post-stream analysis per user decision
9. structlog configured for JSON output with timestamps
</success_criteria>

<output>
After completion, create `.planning/phases/01-event-detection-foundation/01-03-SUMMARY.md`
</output>
